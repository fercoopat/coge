{{!-- template-component.hbs --}}
import { memo } from "react";
import { useTranslation } from "react-i18next";

import {
  FormInputField,
  FormTextareaField,
} from "@/components/forms";
import { FieldGroup } from "@/components/ui/field";

type Props = {
  isLoading: boolean;
};
const {{singularCase (pascalCase name)}}Form = ({ isLoading }: Props) => {
  const { t } = useTranslation();

  return (
    <FieldGroup>
      <FormInputField
        autoFocus
        required
        disabled={isLoading}
        name="name"
        label={t("{{singularCase (camelCase name)}}:fields.name")}
      />

      <FormTextareaField
        disabled={isLoading}
        name="description"
        label={t("{{singularCase (camelCase name)}}:fields.description")}
        rows={5}
      />
    </FieldGroup>
  );
};
export default memo({{singularCase (pascalCase name)}}Form);